{
  "name": "telegram-export",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dump Telegram groups, channels, folders, and forum topics to JSON + markdown.",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "messages": "node --env-file=.env messages.js"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "input": "^1.0.1",
    "telegram": "^2.26.22"
  }
}
